# ---------------------------------------------------------------------------
# Ambientes virtuais (todos os nomes usados no projeto)
# ---------------------------------------------------------------------------
venv/
.venv/
.venv-1/
env/
.env

# ---------------------------------------------------------------------------
# Caches e temporários do Python
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# ---------------------------------------------------------------------------
# Segredos / credenciais (garante que nenhuma API key vaze)
# ---------------------------------------------------------------------------
.pybliometrics/
*.key
secrets.*

# ---------------------------------------------------------------------------
# Dados brutos muito grandes: NÃO versione os CSV pesados, mas MANTENHA o
# manifesto (é a evidência da data de busca do PRISMA) e os READMEs das pastas.
# ---------------------------------------------------------------------------
data/raw/*.csv
data/raw/*.cursor
data/raw/**/*.bib
data/raw/**/*.ris
data/processed/*.csv
!data/raw/_manifest.json
!data/raw/**/README*

# ---------------------------------------------------------------------------
# Saídas pesadas de figuras (regeneráveis); mantenha tabelas e queries (leves,
# úteis como evidência da estratégia de busca).
# ---------------------------------------------------------------------------
results/figures/*.html
